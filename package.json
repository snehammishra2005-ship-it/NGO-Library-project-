{
  "name": "library-system",
  "version": "2.0.0",
  "description": "Two-app library system: a public reader site + a private admin console sharing one Express/Oracle backend. Strictly black & white.",
  "private": true,
  "scripts": {
    "install:all": "npm --prefix backend install && npm --prefix reader-site install && npm --prefix admin-app install",
    "dev:backend": "npm --prefix backend run dev",
    "dev:reader": "npm --prefix reader-site run dev",
    "dev:admin": "npm --prefix admin-app run dev",
    "gen:seed": "npm --prefix backend run gen:seed"
  },
  "license": "ISC"
}
