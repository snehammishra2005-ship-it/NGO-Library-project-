{
  "name": "library-system",
  "version": "3.0.0",
  "description": "Library system delivered as two products: admin-product (backend + staff console, the system of record) and reader-product (public reader site).",
  "private": true,
  "scripts": {
    "install:all": "npm --prefix admin-product/backend install && npm --prefix admin-product/admin-app install && npm --prefix reader-product install",
    "dev:backend": "npm --prefix admin-product/backend run dev",
    "dev:admin": "npm --prefix admin-product/admin-app run dev",
    "dev:reader": "npm --prefix reader-product run dev",
    "gen:seed": "npm --prefix admin-product/backend run gen:seed"
  },
  "license": "ISC"
}
