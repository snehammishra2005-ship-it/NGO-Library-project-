# Admin app config. Copy to `.env` only if you host the admin app SEPARATELY
# from the backend. In local dev the Vite proxy forwards /api/admin to the
# backend on http://localhost:4000.

# Backend origin. Example: VITE_API_BASE=https://api.yourlibrary.com
VITE_API_BASE=
